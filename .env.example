# Préparation Supabase (non branché — formulaire en mode démo)
# Copier vers .env.local pour un usage futur. Le build GitHub Pages
# n'utilise aucune variable d'environnement.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
